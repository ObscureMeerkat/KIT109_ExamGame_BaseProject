// Global game options, set on the title screen and read by levels.
// Static fields persist across scene loads for the whole play session.
public static class GameSettings
{
    public static bool LindsayMode;
    public static bool WindEnabled;
}